<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bug Report - TTH-Bot</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .bug-report-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 30px;
            background: #2b2d31;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.3);
        }
        
        .bug-report-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        
        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        .form-group label {
            color: #b9bbbe;
            font-weight: 600;
            font-size: 14px;
        }
        
        .form-group input,
        .form-group textarea {
            padding: 12px;
            background: #1e1f22;
            border: 1px solid #383a40;
            border-radius: 5px;
            color: #dcddde;
            font-family: inherit;
            font-size: 14px;
        }
        
        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #5865f2;
        }
        
        .form-group textarea {
            min-height: 120px;
            resize: vertical;
        }
        
        .required {
            color: #ed4245;
        }
        
        .submit-btn {
            padding: 12px 30px;
            background: #5865f2;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
        }
        
        .submit-btn:hover {
            background: #4752c4;
        }
        
        .submit-btn:disabled {
            background: #4e5058;
            cursor: not-allowed;
        }
        
        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px;
            background: #1e1f22;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        
        .user-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
        }
        
        .user-details {
            color: #dcddde;
        }
        
        .user-tag {
            font-weight: 600;
            font-size: 16px;
        }
        
        .user-id {
            font-size: 12px;
            color: #b9bbbe;
        }
        
        .back-link {
            display: inline-block;
            margin-bottom: 20px;
            color: #00b0f4;
            text-decoration: none;
            font-size: 14px;
        }
        
        .back-link:hover {
            text-decoration: underline;
        }
        
        h1 {
            color: #ffffff;
            margin-bottom: 10px;
        }
        
        .description {
            color: #b9bbbe;
            margin-bottom: 30px;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <div class="bug-report-container">
        <a href="/dashboard" class="back-link">← Zurück zum Dashboard</a>
        
        <h1>Bug Report</h1>
        <p class="description">
            Hilf uns, den Bot zu verbessern, indem du Bugs meldest. Bitte fülle alle Felder so detailliert wie möglich aus.
        </p>
        
        <div class="user-info">
            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" 
                 alt="Avatar" 
                 class="user-avatar"
                 onerror="this.src='https://cdn.discordapp.com/embed/avatars/0.png'">
            <div class="user-details">
                <div class="user-tag"><%= user.username %>#<%= user.discriminator %></div>
                <div class="user-id">ID: <%= user.id %></div>
            </div>
        </div>
        
        <form class="bug-report-form" method="POST" action="/report-bug">
            <div class="form-group">
                <label for="title">Titel <span class="required">*</span></label>
                <input type="text" 
                       id="title" 
                       name="title" 
                       placeholder="Kurze Zusammenfassung des Bugs"
                       maxlength="100"
                       required>
            </div>
            
            <div class="form-group">
                <label for="description">Beschreibung <span class="required">*</span></label>
                <textarea id="description" 
                          name="description" 
                          placeholder="Detaillierte Beschreibung des Problems"
                          maxlength="1000"
                          required></textarea>
            </div>
            
            <div class="form-group">
                <label for="steps">Schritte zur Reproduktion</label>
                <textarea id="steps" 
                          name="steps" 
                          placeholder="1. Gehe zu...&#10;2. Klicke auf...&#10;3. Beobachte..."
                          maxlength="500"></textarea>
            </div>
            
            <div class="form-group">
                <label for="expected">Erwartetes Verhalten</label>
                <textarea id="expected" 
                          name="expected" 
                          placeholder="Was sollte passieren?"
                          maxlength="500"></textarea>
            </div>
            
            <div class="form-group">
                <label for="actual">Tatsächliches Verhalten</label>
                <textarea id="actual" 
                          name="actual" 
                          placeholder="Was passiert stattdessen?"
                          maxlength="500"></textarea>
            </div>
            
            <button type="submit" class="submit-btn">Bug Report Absenden</button>
        </form>
    </div>
    
    <script>
        const form = document.querySelector('.bug-report-form');
        const submitBtn = document.querySelector('.submit-btn');
        
        form.addEventListener('submit', async (e) => {
            e.preventDefault();
            
            console.log('🐛 Bug Report Form submitted');
            submitBtn.disabled = true;
            submitBtn.textContent = 'Wird gesendet...';
            
            try {
                const formData = new FormData(form);
                const data = Object.fromEntries(formData);
                
                console.log('📤 Sending bug report:', data);
                
                const response = await fetch('/dashboard/report-bug', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(data)
                });
                
                console.log('📡 Response status:', response.status);
                
                if (response.ok) {
                    const result = await response.json();
                    console.log('✅ Bug Report created:', result);
                    alert(`Bug Report erfolgreich gesendet! ID: ${result.bugId}`);
                    window.location.href = '/dashboard';
                } else {
                    const error = await response.json().catch(() => ({ error: 'Server Error' }));
                    console.error('❌ Server Error:', error);
                    alert(`Fehler: ${error.error || 'Unbekannter Fehler'}`);
                    submitBtn.disabled = false;
                    submitBtn.textContent = 'Bug Report Absenden';
                }
            } catch (error) {
                console.error('❌ Network Error:', error);
                alert('Fehler beim Senden des Bug Reports: ' + error.message);
                submitBtn.disabled = false;
                submitBtn.textContent = 'Bug Report Absenden';
            }
        });
    </script>

    <footer style="margin-top: 50px; padding: 30px 0; background: #f8f9fa; text-align: center;">
        <div style="max-width: 1400px; margin: 0 auto; padding: 0 20px;">
            <p style="margin: 0; color: #6c757d; font-size: 14px;">&copy; 2025 TTH-Bot Dashboard • Made with <i class="fas fa-heart" style="color: #ff6b6b;"></i> by Thunderflash007</p>
        </div>
    </footer>
</body>
</html>
