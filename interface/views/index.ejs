<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TTH-Bot - Dein All-in-One Discord Bot</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #0a0e27;
            color: #fff;
            overflow-x: hidden;
        }

        /* Animated Background */
        .animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 50%, #2a1f3a 100%);
        }

        .animated-bg canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        /* Navbar */
        .navbar {
            background: rgba(26, 31, 58, 0.95);
            backdrop-filter: blur(10px);
            padding: 20px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 2px solid rgba(102, 126, 234, 0.3);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .nav-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .nav-brand {
            font-size: 28px;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 12px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .nav-brand i {
            font-size: 32px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .nav-links {
            display: flex;
            gap: 15px;
        }

        .btn {
            padding: 12px 30px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.2);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        /* Hero Section */
        .hero {
            padding: 100px 20px;
            text-align: center;
            position: relative;
        }

        .hero h1 {
            font-size: 4em;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: fadeInUp 1s ease-out;
        }

        .hero .subtitle {
            font-size: 1.5em;
            margin-bottom: 40px;
            color: rgba(255, 255, 255, 0.8);
            animation: fadeInUp 1s ease-out 0.2s both;
        }

        .hero-stats {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin: 60px 0;
            animation: fadeInUp 1s ease-out 0.4s both;
        }

        .stat-box {
            background: rgba(102, 126, 234, 0.1);
            border: 2px solid rgba(102, 126, 234, 0.3);
            padding: 30px 50px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            transition: all 0.3s;
        }

        .stat-box:hover {
            transform: translateY(-5px);
            border-color: #667eea;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }

        .stat-box .number {
            font-size: 3em;
            font-weight: bold;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            display: block;
            margin-bottom: 10px;
        }

        .stat-box .label {
            font-size: 1.1em;
            color: rgba(255, 255, 255, 0.7);
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin: 40px 0;
            animation: fadeInUp 1s ease-out 0.6s both;
        }

        .btn-large {
            padding: 18px 45px;
            font-size: 1.2em;
        }

        .btn-discord {
            background: #5865F2;
        }

        .btn-discord:hover {
            background: #4752C4;
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(88, 101, 242, 0.4);
        }

        /* Features Grid */
        .features-section {
            padding: 80px 20px;
            background: rgba(26, 31, 58, 0.5);
        }

        .section-title {
            text-align: center;
            font-size: 3em;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-subtitle {
            text-align: center;
            font-size: 1.3em;
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 60px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .feature-card {
            background: rgba(102, 126, 234, 0.05);
            border: 2px solid rgba(102, 126, 234, 0.2);
            padding: 35px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            transition: all 0.3s;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            opacity: 0;
            transition: opacity 0.3s;
            z-index: -1;
        }

        .feature-card:hover::before {
            opacity: 0.1;
        }

        .feature-card:hover {
            transform: translateY(-8px) scale(1.02);
            border-color: #667eea;
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.4);
        }

        .feature-card i {
            font-size: 3.5em;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            display: block;
        }

        .feature-card h3 {
            font-size: 1.5em;
            margin-bottom: 15px;
            color: #fff;
        }

        .feature-card p {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
        }

        /* Highlights Section */
        .highlights {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 80px 20px;
            text-align: center;
        }

        .highlights-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .highlight-card {
            background: rgba(255, 255, 255, 0.1);
            padding: 40px 30px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s;
        }

        .highlight-card:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        }

        .highlight-card i {
            font-size: 4em;
            margin-bottom: 20px;
            display: block;
            animation: float 3s ease-in-out infinite;
        }

        .highlight-card h3 {
            font-size: 1.8em;
            margin-bottom: 15px;
        }

        .highlight-card p {
            font-size: 1.1em;
            opacity: 0.9;
        }

        /* Commands Showcase */
        .commands-section {
            padding: 80px 20px;
            background: rgba(26, 31, 58, 0.5);
        }

        .commands-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            max-width: 1200px;
            margin: 40px auto 0;
        }

        .command-item {
            background: rgba(102, 126, 234, 0.1);
            border-left: 4px solid #667eea;
            padding: 20px;
            border-radius: 8px;
            transition: all 0.3s;
        }

        .command-item:hover {
            background: rgba(102, 126, 234, 0.2);
            transform: translateX(10px);
        }

        .command-item .command-name {
            font-size: 1.2em;
            font-weight: bold;
            color: #667eea;
            font-family: 'Courier New', monospace;
            margin-bottom: 8px;
        }

        .command-item .command-desc {
            color: rgba(255, 255, 255, 0.7);
        }

        /* Footer */
        footer {
            background: rgba(10, 14, 39, 0.95);
            padding: 40px 20px;
            text-align: center;
            border-top: 2px solid rgba(102, 126, 234, 0.3);
        }

        footer p {
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.1em;
        }

        footer i.fa-heart {
            color: #e74c3c;
            animation: heartbeat 1.5s ease-in-out infinite;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-15px);
            }
        }

        @keyframes heartbeat {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.2);
            }
        }

        @keyframes glow {
            0%, 100% {
                box-shadow: 0 0 20px rgba(102, 126, 234, 0.5);
            }
            50% {
                box-shadow: 0 0 40px rgba(102, 126, 234, 0.8);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5em;
            }

            .hero .subtitle {
                font-size: 1.2em;
            }

            .hero-stats {
                flex-direction: column;
                gap: 20px;
            }

            .stat-box {
                padding: 20px 30px;
            }

            .features-grid, .highlights-grid, .commands-grid {
                grid-template-columns: 1fr;
            }

            .cta-buttons {
                flex-direction: column;
                padding: 0 20px;
            }

            .btn-large {
                width: 100%;
            }
        }

        /* Scroll Animation */
        .scroll-fade {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease-out;
        }

        .scroll-fade.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="animated-bg">
        <canvas id="particles"></canvas>
    </div>

    <nav class="navbar">
        <div class="container">
            <div class="nav-content">
                <div class="nav-brand">
                    <i class="fas fa-robot"></i>
                    <span>TTH-Bot</span>
                </div>
                <div class="nav-links">
                    <% if (user) { %>
                        <a href="/dashboard" class="btn btn-primary">
                            <i class="fas fa-tachometer-alt"></i> Dashboard
                        </a>
                        <a href="/auth/logout" class="btn btn-secondary">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </a>
                    <% } else { %>
                        <a href="/auth/discord" class="btn btn-primary">
                            <i class="fab fa-discord"></i> Mit Discord anmelden
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <h1><i class="fas fa-robot"></i> TTH-Bot</h1>
            <p class="subtitle">Dein professioneller All-in-One Discord Bot mit umfangreichem Web-Dashboard!</p>
            
            <div class="hero-stats">
                <div class="stat-box">
                    <span class="number" id="feature-count">50+</span>
                    <span class="label">Features</span>
                </div>
                <div class="stat-box">
                    <span class="number">24/7</span>
                    <span class="label">Online</span>
                </div>
                <div class="stat-box">
                    <span class="number">&lt;1s</span>
                    <span class="label">Response Time</span>
                </div>
            </div>

            <div class="cta-buttons">
                <% if (!user) { %>
                    <a href="/auth/discord" class="btn btn-large btn-discord">
                        <i class="fab fa-discord"></i> Jetzt mit Discord verbinden
                    </a>
                <% } else { %>
                    <a href="/dashboard" class="btn btn-large btn-primary">
                        <i class="fas fa-tachometer-alt"></i> Zum Dashboard
                    </a>
                <% } %>
                <a href="#features" class="btn btn-large btn-secondary">
                    <i class="fas fa-info-circle"></i> Mehr erfahren
                </a>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="features-section scroll-fade">
        <div class="container">
            <h2 class="section-title"><i class="fas fa-star"></i> Features</h2>
            <p class="section-subtitle">Alles was dein Discord-Server braucht</p>
            
            <div class="features-grid">
                <div class="feature-card">
                    <i class="fas fa-shield-alt"></i>
                    <h3>Moderation</h3>
                    <p>Leistungsstarke Moderationstools mit Warn-, Ban-, Kick- und Mute-System. Automatisches Logging aller Aktionen.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-ticket-alt"></i>
                    <h3>Ticket-System</h3>
                    <p>Professionelles Support-Ticket-System mit Claim, Assign, Prioritäten und Live-Übersicht im Dashboard.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-chart-line"></i>
                    <h3>Level-System</h3>
                    <p>XP & Level-System mit customizable Belohnungen und automatischen Rollen bei Level-Up.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-users"></i>
                    <h3>Willkommenssystem</h3>
                    <p>Automatische Begrüßung neuer Mitglieder mit personalisierten Nachrichten und Variablen.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-user-shield"></i>
                    <h3>Team-Management</h3>
                    <p>Organisiere dein Team mit verschiedenen Rängen von Owner bis Trial-Supporter.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-clock"></i>
                    <h3>Scheduled Messages</h3>
                    <p>Automatische Nachrichten - täglich, wöchentlich oder in benutzerdefinierten Intervallen.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-user-plus"></i>
                    <h3>Auto-Rollen</h3>
                    <p>Automatische Rollenvergabe bei Join, Level-Up, nach Zeit oder bei Server-Boost.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-code"></i>
                    <h3>Custom Commands</h3>
                    <p>Erstelle eigene Commands mit Variablen, Embeds und detaillierten Nutzungsstatistiken.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-bullhorn"></i>
                    <h3>Quick Announcements</h3>
                    <p>Sende Ankündigungen schnell in mehrere Channels mit Mentions und Custom-Embeds.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-file-alt"></i>
                    <h3>Bewerbungssystem</h3>
                    <p>Strukturierte Team-Bewerbungen mit Modal-Forms und automatischer Verwaltung.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-database"></i>
                    <h3>Backup & Export</h3>
                    <p>Vollständige Backups und Export aller wichtigen Daten für maximale Sicherheit.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-cogs"></i>
                    <h3>Web-Dashboard</h3>
                    <p>Übersichtliches und modernes Web-Interface zur kompletten Bot-Verwaltung.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Highlights Section -->
    <section class="highlights scroll-fade">
        <div class="container">
            <h2 style="font-size: 3em; margin-bottom: 20px;">
                <i class="fas fa-star"></i> Warum TTH-Bot?
            </h2>
            <p style="font-size: 1.3em; margin-bottom: 50px; opacity: 0.9;">
                Was uns besonders macht
            </p>
            
            <div class="highlights-grid">
                <div class="highlight-card">
                    <i class="fas fa-bolt"></i>
                    <h3>Blitzschnell</h3>
                    <p>Optimierte Performance für sofortige Reaktionszeiten unter 1 Sekunde</p>
                </div>
                <div class="highlight-card">
                    <i class="fas fa-paint-brush"></i>
                    <h3>Modernes Design</h3>
                    <p>Schöne Embeds und übersichtliches Dashboard mit intuitiver Bedienung</p>
                </div>
                <div class="highlight-card">
                    <i class="fas fa-sliders-h"></i>
                    <h3>Hochgradig konfigurierbar</h3>
                    <p>Passe jeden Aspekt des Bots nach deinen individuellen Wünschen an</p>
                </div>
                <div class="highlight-card">
                    <i class="fas fa-server"></i>
                    <h3>24/7 Verfügbar</h3>
                    <p>Zuverlässig und stabil - rund um die Uhr für deinen Server erreichbar</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Commands Showcase -->
    <section class="commands-section scroll-fade">
        <div class="container">
            <h2 class="section-title"><i class="fas fa-terminal"></i> Beliebte Commands</h2>
            <p class="section-subtitle">Eine Auswahl der meist genutzten Funktionen</p>
            
            <div class="commands-grid">
                <div class="command-item">
                    <div class="command-name">/ticket</div>
                    <div class="command-desc">Erstelle ein neues Support-Ticket</div>
                </div>
                <div class="command-item">
                    <div class="command-name">/warn</div>
                    <div class="command-desc">Verwarnung an einen User aussprechen</div>
                </div>
                <div class="command-item">
                    <div class="command-name">/level</div>
                    <div class="command-desc">Zeige dein aktuelles Level und XP</div>
                </div>
                <div class="command-item">
                    <div class="command-name">/vorschlag</div>
                    <div class="command-desc">Reiche einen Vorschlag ein</div>
                </div>
                <div class="command-item">
                    <div class="command-name">/projekt</div>
                    <div class="command-desc">Verwalte Server-Projekte</div>
                </div>
                <div class="command-item">
                    <div class="command-name">/serverinfo</div>
                    <div class="command-desc">Zeige Informationen über den Server</div>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="container">
            <p>&copy; 2025 TTH-Bot Dashboard • Made with <i class="fas fa-heart"></i> by Thunderflash007</p>
        </div>
    </footer>

    <script>
        // Particle Animation
        const canvas = document.getElementById('particles');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        const particles = [];
        const particleCount = 100;

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.vx = (Math.random() - 0.5) * 0.5;
                this.vy = (Math.random() - 0.5) * 0.5;
                this.radius = Math.random() * 2 + 1;
            }

            update() {
                this.x += this.vx;
                this.y += this.vy;

                if (this.x < 0 || this.x > canvas.width) this.vx *= -1;
                if (this.y < 0 || this.y > canvas.height) this.vy *= -1;
            }

            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(102, 126, 234, 0.5)';
                ctx.fill();
            }
        }

        for (let i = 0; i < particleCount; i++) {
            particles.push(new Particle());
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            
            particles.forEach(particle => {
                particle.update();
                particle.draw();
            });

            // Draw connections
            particles.forEach((p1, i) => {
                particles.slice(i + 1).forEach(p2 => {
                    const dx = p1.x - p2.x;
                    const dy = p1.y - p2.y;
                    const distance = Math.sqrt(dx * dx + dy * dy);

                    if (distance < 100) {
                        ctx.beginPath();
                        ctx.moveTo(p1.x, p1.y);
                        ctx.lineTo(p2.x, p2.y);
                        ctx.strokeStyle = `rgba(102, 126, 234, ${1 - distance / 100})`;
                        ctx.lineWidth = 0.5;
                        ctx.stroke();
                    }
                });
            });

            requestAnimationFrame(animate);
        }

        animate();

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        });

        // Scroll Animation
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.scroll-fade').forEach(el => {
            observer.observe(el);
        });

        // Animated Counter
        const featureCount = document.getElementById('feature-count');
        let count = 0;
        const target = 50;
        const duration = 2000;
        const increment = target / (duration / 16);

        function updateCounter() {
            count += increment;
            if (count < target) {
                featureCount.textContent = Math.floor(count) + '+';
                requestAnimationFrame(updateCounter);
            } else {
                featureCount.textContent = target + '+';
            }
        }

        // Start counter when visible
        const heroObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    updateCounter();
                    heroObserver.disconnect();
                }
            });
        });

        heroObserver.observe(document.querySelector('.hero'));

        // Smooth Scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
</body>
</html>
